User-agent: *
Allow: /

# /result and /saved are excluded so search engines don't index user
# state — they're noindex via meta tag too.
Disallow: /result
Disallow: /saved
Disallow: /settings

# Pro app surface (auth-walled, no public content) and JSON API routes
# (POST-only — GET-crawling them is wasted budget).
Disallow: /app/
Disallow: /api/

Sitemap: https://fairshare.guigalabs.com/sitemap.xml
